#!/bin/sh
# Fail the commit unless the unit test suite is green.
# Prefer Docker so local Go is not required (see AGENTS.md / Makefile).
set -e
cd "$(git rev-parse --show-toplevel)"
echo "pre-commit: running make test-docker..."
make test-docker
